{
  "name": "oneorg-registration",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist/*",
    "build": "tsc --build",
    "test": "sudo node --inspect dist",
    "start": "sudo node dist",
    "regen-keys": "node keys/gen.js"
  },
  "devDependencies": {
    "@types/dns2": "^2.0.3",
    "@types/node": "^20.1.0"
  },
  "dependencies": {
    "dnsz": "^4.0.0",
    "ip6addr": "^0.2.5",
    "typescript": "^5.1.6"
  }
}
